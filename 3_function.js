// functions involkation and variable environment

// A function in JavaScript is a reusable block of code that performs a specific task or set of tasks. 

/*
function functionName(parameters) {
  // Function body: code that defines what the function does
  // It can use the parameters as inputs and perform operations
  // It may also return a value using the return statement
}

function add(a, b) {
  return a + b;
}

Functions in JavaScript can also be assigned to variables, passed as arguments to other functions (a concept known as higher-order functions), and even returned as values from other functions

*/

var x = 1;
a();
b();

console.log(x);

function a() {
  var x = 10;
  console.log(x);
}

function b() {
  var x = 100;
  console.log(x);
}

// output :
// 1
// 10
// 100